# TODO - Student Management Portal (Bootstrap 5)

- [ ] Create shared folder structure: `css/` and `js/`.
- [ ] Create `css/styles.css` for small custom styles.
- [ ] Create `index.html` (Home) with: Bootstrap navbar (dropdown + login), hero, carousel, features cards, accordion, modal, footer with social links.
- [ ] Create `register.html` (Student Registration Form) with: full fields, input groups, validation classes, upload photo, terms checkbox.
- [ ] Create `dashboard.html` (Student Dashboard) with: statistics cards, search box, responsive table, badges, accordion.
- [ ] Create `profile.html` (Student Profile) with: profile card, badges, buttons.
- [ ] Create `contact.html` (Contact Us) with: form, alerts, bootstrap form validation.
- [ ] Create `js/main.js` for dashboard search (and any small UI hooks).
- [ ] Sanity-check by running minimal local checks (open `index.html` in browser).

